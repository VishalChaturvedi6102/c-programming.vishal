// C-program to find the number of apples given by each person without adding.
#include <stdio.h>
int main()
{
    float c,a,mul;
    c = 3;
    a = 6.5;
    mul = (c*a);
    printf("The number of apples are :  %f",mul);
    return 0;
}